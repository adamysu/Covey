# Scripts

This folder is reserved for operational scripts such as:

- Prototype JSON export/import helpers
- Database backup helpers
- Database restore helpers
- One-time data cleanup tasks

Prefer checked-in scripts over manual one-off commands once real flock data exists.
